---
title: 故障排查
description: 常见问题及解决方法。
navigation:
  title: 故障排查
  icon: i-lucide-life-buoy
seo:
  title: "故障排查"
  description: "常见问题及解决方法。"
---
## 找不到 `@eN` 引用

自上次 `snapshot` 以来页面已变化。重新快照并使用新引用：

```bash
omniscout browser snapshot --refs-only
omniscout browser click '@e7'
```

## 登录一直无法完成

从另一个 shell 发送 `--done` 信号：

```bash
# 终端 A：
omniscout browser login https://app.example.com --profile work

# 终端 B（登录完成后）：
omniscout browser login --done
```

## 验证码一直阻塞

在有界面窗口中手动解决，或使用第三方解决服务：

```bash
export TWOCAPTCHA_API_KEY=...
omniscout browser captcha --solver 2captcha
```

## 扩展提示 `backend_unavailable`

1. 打开 `chrome://extensions`——确认扩展已加载
2. 点击扩展图标——如果断开请重连
3. 或去掉 `--backend extension` 改用 Playwright

## Python 版本报错

```bash
python --version  # 需要 3.11+
pip install omniscout
```

## 找不到 Chrome

```bash
omniscout settings browsers          # 查看已安装的浏览器
omniscout settings set browser edge  # 或 brave、vivaldi 等
```

## Daemon 无法启动

```bash
omniscout daemon stop
omniscout daemon start
lsof -i :7720                       # 检查端口是否被占用
omniscout daemon start --port 7721   # 使用其他端口
```

## 搜索无结果

```bash
omniscout search "more specific query"
omniscout search "query" --source ddg
```

## 截图空白

```bash
# 等待加载
omniscout browser screenshot https://example.com --delay 3 --out /tmp/page.png

# 整页
omniscout browser screenshot https://example.com --full-length --out /tmp/full.png
```

## 常见问题

**我的数据会被发送到云端吗？**
不会。唯一的出站流量是 DuckDuckGo 搜索和您明确获取的 URL。一次性模型下载会缓存在本地。

**不用 Chrome 可以使用 OmniScout 吗？**
需要 Chromium 系浏览器。安装 Chrome/Chromium，或用 `omniscout install --browser custom --browser-executable <path>` 配置自定义路径。

**如何更新？**
```bash
pip install --upgrade omniscout
```

**如何清除所有数据？**
```bash
rm -rf ~/Library/Application\ Support/omniscout/   # macOS
rm -rf ~/.local/share/omniscout/                   # Linux
```

**去哪里获取帮助？**
- [命令行概览](/zh-CN/cli/overview)
- [示例](/zh-CN/cli/examples)
- [GitHub issues](https://github.com/sriramramnath/omniscout/issues)
