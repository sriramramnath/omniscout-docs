---
title: 빠른 시작
description: 5분 만에 첫 검색, 추출, 브라우저 자동화를 해보세요.
icon: Rocket
---

이 가이드를 따라 약 5분 만에 브라우저 자동화까지 동작시킬 수 있습니다. 먼저 [설치](/docs/installation)를 완료했다고 가정합니다.

## 1. Daemon 시작

```bash
omniscout daemon start
```

## 2. 웹에서 답변 얻기

`answer`는 사실을 가장 빠르게 얻는 방법입니다 — 검색 후 로컬에서 리랭크하고 출처와 함께 합성합니다:

```bash
omniscout answer "what is the capital of france"
```

랭크된 링크 목록만 보고 싶으신가요? `search`를 사용하세요:

```bash
omniscout search "local-first AI agents" --limit 5
```

출처, depth 모드, answer 엔진에 대해서는 [검색 및 답변](/docs/search/search)을 참고하세요.

## 3. 페이지 추출

`extract`는 어떤 URL이든 깨끗한 Markdown으로 바꿔줍니다 — 브라우저가 필요 없습니다:

```bash
omniscout extract https://example.com
```

Markdown 대신 특정 필드를 JSON으로 받고 싶으신가요? [추출](/docs/search/extraction)을 참고하세요.

## 4. 브라우저 구동

대부분의 브라우저 작업은 네 명령어로 충분합니다: 페이지로 이동하고, 상호작용 요소를 snapshot으로 확인하고, `@eN` ref에 동작하고, 결과를 스크린샷으로 캡처합니다:

```bash
omniscout browser navigate https://news.ycombinator.com
omniscout browser snapshot --refs-only
omniscout browser click '@e3'
omniscout browser screenshot --out /tmp/hn.png
```

핵심 규칙: **snapshot이 `@eN` ref를 반환하고, 모든 동작은 ref를 대상으로 합니다.** 내비게이션 후에는 다시 snapshot하세요 — 페이지가 바뀌면 ref가 무효화됩니다. 전체 동작 목록은 [브라우저 자동화](/docs/automation/browser)를 참고하세요.

## 5. 머신이 읽을 수 있는 출력 만들기

모든 명령어는 `--json`을 지원하며, 에이전트는 항상 사용해야 합니다:

```bash
omniscout --json answer "What is local-first software?"
export OMNISCOUT_JSON=1   # same effect for every command
```

## 6. 정리

```bash
omniscout browser close --all
```

## 치트 시트

```bash
omniscout daemon start                              # once per machine
omniscout answer "..."                              # Q&A with citations
omniscout extract https://example.com               # page → Markdown
omniscout browser navigate <url>                    # open a page
omniscout browser snapshot --refs-only              # list @eN refs
omniscout browser click '@e3'                       # act on a ref
omniscout browser screenshot --out /tmp/out.png     # capture state
omniscout browser close --all                       # clean up
```

## 다음 단계

- [검색 및 답변](/docs/search/search) — 출처, 리랭킹, depth, 엔진
- [브라우저 자동화](/docs/automation/browser) — 폼, 탭, 네트워크, 로그인, CAPTCHA
- [예제 및 레시피](/docs/examples) — 복사해서 쓰는 워크플로
